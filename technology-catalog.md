# Technology Inventory — Last 12 Months (Aug 2025 – Jul 2026)

**Source:** 67 distinct projects across GitHub (`github.com/hankazarian/*`), `/Users/hankazarian/Documents/antigravity/`, and `/Users/hankazarian/Desktop/projects/`  
**Audited:** 2026-07-27  
**Counting rule:** Each distinct repository, client system, product, or independently governed implementation counted once. Routes, branches, PRs, and subdirectories within the same project are not counted separately.

---

## Technology Inventory

| Technology / Platform | Category | Projects | Named Projects | Primary Role | Key Selection Rationale | Proof / Evidence | Confidence |
|---|---|---|---|---|---|---|---|
| **Astro** | Frontend framework | 20 | hankazarian.com-beta, Delta-Graph, CineCurate, token-roulette, growth-sprint-engine, The-SuperFair, ai-seo-testing, fair-manager, ai-visibility-tools, VentureVetter, google-ai-solar-system, cougar-gamehub, fantastic-careers, media-kit, Nikolas-battle-arena, TheRecipePantry, What-the-OKF, 1428 Brickell, Helix-web, PatentPlayer, SmartWardrobe | Static site generation, SSR on Cloudflare Workers or Node, route-based page architecture with selective component hydration | Static-first output with per-route hydration control; native Cloudflare Workers adapter enables SSR without separate Node server (CineCurate, hankazarian.com-beta); Node adapter for projects needing server-side SQLite (SmartWardrobe); `astro:content` for type-safe content collections | `package.json` (astro dep), `astro.config.mjs` (output mode, integrations, adapter, base path), `.astro` page components across all 20 repos | High |
| **Svelte 5** | UI framework | 22 | hankazarian.com-beta, Delta-Graph, SEOTuningFork, CineCurate, The-SuperFair, ai-seo-testing, fair-manager, ai-visibility-tools, VentureVetter, NativeEar, alignment-sphere, pocket-retailer, cac-trap-analyzer-v2, cougar-gamehub, media-kit, Nikolas-battle-arena, TheRecipePantry, What-the-OKF, 1428 Brickell, Helix-web, PatentPlayer, SmartWardrobe | Interactive component islands inside Astro pages; standalone SvelteKit apps for mobile and web | Lightweight client bundle (no virtual DOM, compiled away) for selective interactivity inside otherwise static Astro pages; Svelte 5 Runes (`$state`, `$derived`, `$effect`) simplify state management without boilerplate; SvelteKit adapter-static for Capacitor-driven mobile apps (NativeEar); adapter-node for SSR apps (cac-trap-analyzer-v2, SmartWardrobe) | `package.json` (svelte dep), `svelte.config.js`, `.svelte` component files, Svelte 5 Runes syntax (`$state`, `$props`) in component source, `@astrojs/svelte` integration in `astro.config.mjs` | High |
| **React 19** | UI framework | 15 | 1414-3rd, anna-elizabeth-website, property-management-demo, google-ai-solar-system, kamalame-2025, hairstyler-AI, RecipeDiff, localminds-ai, browse-local, pocket-retailer (root), media-kit (legacy), icp-builder-pro, krelwear-storefront, soccer_app, MantlestoneCapital, Root Phaser game | Interactive SPA, data dashboards, AI chat interfaces, storefront, game UI | Mature ecosystem for complex client-side state; MapLibre GL has first-class React bindings (1414-3rd); Next.js projects (kamalame-2025, art-basel-guide, MantlestoneCapital) require React by definition; Phaser game framework uses React for UI overlay scenes | `package.json` (react, react-dom deps), `vite.config.ts` (`@vitejs/plugin-react`), JSX in `.tsx` files, `next.config.ts` for Next.js projects | High |
| **Next.js 15 / 16** | Frontend framework | 3 | kamalame-2025, art-basel-guide, MantlestoneCapital | SSR web application with file-based routing, Supabase integration | Required for product landing pages needing SSR with dynamic OG images and SEO meta (kamalame-2025); Turbopack for faster dev iteration; standalone output for Docker (art-basel-guide); Supabase SSR auth (MantlestoneCapital) | `next.config.ts` or `next.config.js`, `package.json` (next dep), `postcss.config.mjs`, `tailwind.config.ts`, `app/` directory router files | High |
| **Angular 21** | Frontend framework | 1 | MercNakos | SPA with reactive forms and API integration | Chosen for marine marketplace specifically; Angular's built-in HTTP client and reactive forms suited the listing/submission workflow; RxJS for managing concurrent API calls | `angular.json`, `tsconfig.json`, `metadata.json`, `@angular/core` in `package.json` | High |
| **SvelteKit 2** | Full-stack framework | 4 | SEOTuningFork, cac-trap-analyzer-v2, NativeEar, alignment-sphere | Standalone web apps with SSR, mobile apps via Capacitor, SPA with 3D | Adapter-node (cac-trap-analyzer-v2) for SSR deployment behind nginx; adapter-static (NativeEar) for Capacitor mobile build; SPA fallback mode (alignment-sphere) for Three.js heavy pages | `@sveltejs/kit` in `package.json`, `svelte.config.js` (adapter config), `vite.config.ts` for SvelteKit | High |
| **TanStack Start** | Full-stack framework | 1 | media-kit (legacy) | SSR React application | React-based SSR with streaming; TanStack Router for type-safe routing; deployed via Nitro server | `@tanstack/react-start` in `package.json`, `vite.config.ts` with `@lovable.dev/vite-tanstack-config`, `bunfig.toml` | Medium |
| **TypeScript** | Language | 55+ | Nearly all repos except vanilla HTML/JS projects (seo-bingo, the-great-decoupling, ai-use-disclosure-prototype, Impossible-Miami, Growth-Sprint-New, LI, LinkedIn Outreach) | Type-safe application code across all tiers | Required for maintainable codebase across 67+ projects; enables strict mode (`astro/tsconfigs/strict`) across all Astro projects; `tsconfig.json` present in every non-vanilla repo | `tsconfig.json` or `tsconfig.app.json` in each project, `typescript` in `devDependencies`, `.ts`/`.tsx` source files | High |
| **Python 3.12** | Language | 15 | overstock-cellars, the-recipe-rebel-growth, three-surface-sdd-runner, AIVis-Bench, RecordsFetcher, research-buddy, Linkedin-seo-this-week, linkedin-content-monitor, project-gallery, linkedin-suite, grandandco-crm, propertychecker-eval, LinkedIn Outreach, LI, anna-elizabeth-website | CLI tools, AI agent orchestration, web backends, data pipelines, LLM evaluation, admin tools | Ubiquitous AI/LLM library support (pydantic-ai, OpenAI, Anthropic, Google GenAI); fast prototyping of data-intensive workflows; rich CLI ecosystem (Typer, Rich); Pydantic for type-safe data handling; Flask for lightweight admin UIs (LI) | `requirements.txt`, `pyproject.toml` (Python 3.12+ classifier), `.py` source files, `Python` in GitHub language stats | High |
| **Tailwind CSS 4** | CSS framework | 14 | hankazarian.com-beta, Delta-Graph, SEOTuningFork, token-roulette, property-management-demo, alignment-sphere, NativeEar, cac-trap-analyzer-v2, MercNakos, kamalame-2025, art-basel-guide, media-kit (legacy), icp-builder-pro, krelwear-storefront | Utility-first CSS for rapid UI development | Eliminates custom CSS files for common patterns; CSS-first configuration (v4) removes `tailwind.config.js` boilerplate; Tailwind CSS vite/postcss plugin integrates with both Astro and Vite builds | `tailwindcss` in `package.json`, `@tailwindcss/vite` or `@tailwindcss/postcss` config, `@import "tailwindcss"` in CSS entry file, `wrangler.jsonc` for CF projects | High |
| **Tailwind CSS 3** | CSS framework | 5 | MantlestoneCapital, icp-builder-pro, kamalame-2025, Root Phaser game, alignment-sphere | Utility-first CSS (legacy config-based) | Chosen in older projects before v4 release; JavaScript config file for custom theme extensions (MantlestoneCapital color palette) | `tailwind.config.js` or `tailwind.config.ts`, `tailwindcss` in `package.json`, PostCSS plugin config | High |
| **Vanilla CSS** | CSS framework | 22 | 1414-3rd, cougar-gamehub, fantastic-careers, media-kit (main), Nikolas-battle-arena, TheRecipePantry, What-the-OKF, ai-seo-testing, fair-manager, ai-visibility-tools, project-gallery, the-great-decoupling, ai-use-disclosure-prototype, seo-bingo, linkedin-wrapped-2025, linkedin-content-monitor, overstock-cellars, growth-sprint-engine, Impossible-Miami, LI, LinkedIn Outreach, 1428 Brickell | Custom CSS with no framework dependency | Deliberate choice in projects with strict design governance (1414-3rd HUG CSS doctrine); avoids framework lock-in for long-lived marketing sites; smaller CSS payload for content-focused sites; WordPress child themes inherit parent theme styles | `.css` files present, no `tailwindcss` or other CSS framework in `package.json`, CSS custom properties and layered stylesheets in source | High |
| **Vite** | Build tool | 16 | 1414-3rd, anna-elizabeth-website, SEOTuningFork, hairstyler-AI, RecipeDiff, localminds-ai, browse-local, linkedin-wrapped-2025, cac-trap-analyzer-v2, localminds-ai, browse-local, media-kit (legacy), icp-builder-pro, krelwear-storefront, soccer_app, Root Phaser game | JavaScript/TypeScript bundler and dev server | Fast HMR for React SPAs; plugin ecosystem (React, Svelte, Tailwind); used as the underlying bundler for Astro projects; path alias configuration (`@/`) for clean imports; Vitest integration for testing | `vite.config.ts` or `vite.config.js`, `@vitejs/plugin-react` in deps, `vite` in `package.json` | High |
| **Bun** | Package manager / runtime | 2 | property-management-demo, media-kit (legacy) | Fast package installation and script running | Dramatically faster `npm install` for the large shadcn/ui dependency tree; used as package manager only (not runtime) in both projects | `bunfig.toml`, `bun.lock`, `package.json` (`packageManager` field) | High |
| **PNPM / Turborepo** | Monorepo tooling | 1 | linkedin-suite | Monorepo orchestration for multi-package Python/JS repo | PNPM workspace for Node packages + UV for Python packages; Turborepo for parallel task execution across packages (core, council, gallery, outreach, seo-intel) | `pnpm-workspace.yaml`, `turbo.json`, `uv.lock` | High |
| **Cloudflare Workers** | Edge hosting | 4 | hankazarian.com-beta, CineCurate, anna-elizabeth-website, cougar-gamehub (Pages) | Serverless edge execution, static asset serving, KV storage, D1 database | Astro's `@astrojs/cloudflare` adapter outputs Worker-compatible SSR bundles; no cold-start concern for low-traffic sites; free tier handles traffic; KV for session/cache, D1 for relational data (CineCurate) | `wrangler.jsonc` or `wrangler.toml`, `@astrojs/cloudflare` in deps, `astro.config.mjs` with `adapter: cloudflare()`, `routes.json` | High |
| **Docker** | Containerization | 16 | CineCurate, The-SuperFair, anna-elizabeth-website, property-management-demo, alignment-sphere, pocket-retailer, cac-trap-analyzer-v2, research-buddy, art-basel-guide, grandandco-crm, Linkedin-seo-this-week, 1428 Brickell, Helix, krelwear-storefront, PatentPlayer, generatepress-child | Consistent development and deployment environments | Needed for projects with complex service dependencies (multiple backends, databases); isolates Python/Node version requirements; nginx reverse proxy pattern in pocket-retailer for multi-app hosting; WordPress/PHP bundling (generatepress-child); Rust + Node multi-stage (Helix) | `Dockerfile`, `docker-compose.yml`, `.dockerignore` in each repo | High |
| **Promptfoo** | LLM evaluation | 1 | propertychecker-eval | Structured LLM output evaluation across multiple model providers | Repeatable multi-model comparison with assertions; YAML-driven config separates test definitions from code; supports OpenRouter for unified API across GPT-5, Claude, Gemini, Perplexity | `promptfooconfig*.yaml` (multiple eval configs), `prompts/` directory with phase0 prompt files | High |
| **pydantic-ai** | AI agent framework | 2 | the-recipe-rebel-growth, RecordsFetcher | Structured AI agent orchestration with type-safe outputs | Type-safe LLM outputs with Pydantic models; agent-based workflow pattern (filter-transform-aggregate) in the-recipe-rebel-growth; integrated with MCP tools in RecordsFetcher for multi-provider support | `requirements.txt` (`pydantic-ai>=2.0.0`), `.py` source files using `pydantic_ai` imports | High |
| **Pydantic** | Data validation | 6 | overstock-cellars, three-surface-sdd-runner, AIVis-Bench, RecordsFetcher, grandandco-crm, linkedin-suite | Type-safe data models for CLI and backend applications | Zero-overhead data validation for campaign packets (overstock-cellars), SDD workflow state (three-surface-sdd-runner), and benchmark results (AIVis-Bench); integrates with pydantic-ai for LLM agent responses | `requirements.txt` or `pyproject.toml` (`pydantic>=2.0`), `from pydantic import BaseModel` in source files | High |
| **SQLite** | Database | 10 | Delta-Graph, growth-sprint-engine, Linkedin-seo-this-week, RecordsFetcher, linkedin-content-monitor, NativeEar, the-recipe-rebel-growth, SmartWardrobe, LI, LinkedIn Outreach | Embedded local database for single-process applications | Zero-configuration, no separate DB server needed; SQLite WASM enables in-browser querying (Delta-Graph); sqlite-vec extension adds vector search; better-sqlite3 for Node server access (SmartWardrobe) | `better-sqlite3` or `aiosqlite` in deps, SQL schema files, `@sqlite.org/sqlite-wasm` for WASM, `sqlite-vec` for vector search, `.db` files in project | High |
| **SQLite WASM** | In-browser database | 2 | hankazarian.com-beta, Delta-Graph | Client-side SQL queries in the browser | Enables SQL-based data analysis without a backend; loaded via ESM in workers; sqlite-vec companion for vector embedding search on the client | `@sqlite.org/sqlite-wasm` in `package.json`, `sqlite-vec` dep, WASM initialization in source | Medium |
| **Supabase** | Backend-as-a-Service | 1 | MantlestoneCapital | Auth, database, SSR session management | Provides auth + PostgreSQL database + SSR session handling in one service; `@supabase/ssr` package handles cookie-based auth for Next.js; replaces custom auth server | `@supabase/supabase-js`, `@supabase/ssr` in `package.json`, `lib/supabase/` directory with client/server helpers | High |
| **Directus** | Headless CMS | 1 | Directus+Astro | Self-hosted headless CMS for Astro content | Docker-based deployment (v11.15.1); SQLite-backed; provides REST and GraphQL APIs; planned integration with Astro content collections for publishing workflows | `docker-compose.yml` with `directus/directus:11.15.1` image, `.env` with DB config, `AGENTS.md` referencing publishing-astro-websites skill | High |
| **WordPress** | CMS / PHP | 1 | generatepress-child | PHP-based CMS with child theme customization | Client required WordPress; GeneratePress theme as lightweight base; Docker-based dev environment with WP-CLI for automation; child theme for custom PHP templates and CSS overrides | `front-page.php`, `functions.php`, `style.css` in child theme directory, `Dockerfile` with `FROM wordpress:latest` + WP-CLI | High |
| **Shopify** | E-commerce platform | 1 | krelwear-storefront | Shopify theme development and custom storefront | Client store on Shopify; `@shopify/cli` and `@shopify/theme` for local theme development and deployment; custom React 19 storefront (separate from default theme) for enhanced product browsing | `@shopify/cli`, `@shopify/theme` in `package.json`, `krelwear-theme/` directory with Liquid templates, `config/`, `layout/`, `assets/` | High |
| **Phaser 3** | Game framework | 1 | Root Phaser game | 2D game engine for browser-based games | Game framework with scene management, physics, input handling; used for a game project with multiple scene types (battle, arena, chapter, character select); TypeScript-first with Phaser type definitions | `phaser: 3.90.0` in root `package.json`, `src/scenes/` with 20+ scene files, `Phaser` import in `main.ts`, game config | High |
| **OpenGame SDK** | Game development | 1 | opengame-evaluation | SDK for OpenGame game development and evaluation | Framework for building and evaluating OpenGame-compatible games; includes background removal (`@imgly/background-removal-node`) for asset processing; Ink (React terminal) for CLI game interaction | `@opengame/sdk` in `package.json`, `Makefile`, `esbuild.config.js`, `packages/` directory structure | Medium |
| **Rust** | Systems language | 1 | Helix (helix-engine) | High-performance browser automation CLI | Chosen for the Helix engine requiring fast headless Chrome screenshot processing and image manipulation; `clap` for CLI argument parsing, `headless_chrome` crate for browser automation, `image` crate for image processing | `Cargo.toml` with deps (anyhow, clap, headless_chrome, image, serde, regex), `src/` Rust source files, `Cargo.lock` | High |
| **D3.js** | Visualization | 4 | google-ai-solar-system, Delta-Graph, cac-trap-analyzer-v2, 1414-3rd | Custom data visualizations and graph rendering | Required for non-standard chart types (force-directed graphs, solar system layout, Sankey/flow diagrams) not covered by conventional charting libraries; fine-grained SVG control | `d3` in `package.json`, `@types/d3`, `d3-scale` in deps, `import * as d3 from 'd3'` in source | High |
| **Three.js / Threlte** | 3D rendering | 2 | hankazarian.com-beta, alignment-sphere | 3D visualizations and interactive scenes | 3D brand experience for personal site (hankazarian.com-beta); Threlte provides Svelte-native bindings (components, reactivity) avoiding imperative Three.js code | `@threlte/core`, `@threlte/extras`, `three` in `package.json`, Threlte components in source | High |
| **MapLibre GL JS** | Mapping | 1 | 1414-3rd | Interactive vector tile map for real estate property | Only WebGL-based map library that supports custom vector tile styling without a paid API key; required for the 1414-3rd property site's interactive location layer | `maplibre-gl` in `package.json`, map initialization in React components, style JSON | High |
| **Google Gemini API** | LLM API | 9 | VentureVetter, MercNakos, pocket-retailer, hairstyler-AI, RecipeDiff, browse-local, linkedin-content-monitor, RecordsFetcher, icp-builder-pro, LinkedIn Outreach | Content generation, analysis, structured data extraction | Generous free tier during development; direct `@google/genai` SDK for TypeScript/React projects; used for recipe comparison (RecipeDiff), startup analysis (VentureVetter), content monitoring (linkedin-content-monitor), and ICP page generation (icp-builder-pro) | `@google/genai` in `package.json` or `google-genai` in `requirements.txt`, API client initialization in source, `.env.example` with `GEMINI_API_KEY` | High |
| **OpenAI API** | LLM API | 3 | RecordsFetcher, linkedin-content-monitor, LinkedIn Outreach | Content analysis, structured data extraction, outreach messaging | Standard choice for general-purpose LLM tasks; used alongside other providers in RecordsFetcher's multi-provider workflow; powers outreach message generation in LinkedIn Outreach | `openai` in `requirements.txt`, `from openai import OpenAI` in source, `.env` references to `OPENAI_API_KEY` | High |
| **Anthropic API** | LLM API | 1 | RecordsFetcher | Long-context document analysis | Claude's 200K context window needed for processing large records documents; used as one provider in the multi-provider RecordsFetcher workflow | `anthropic` imports in Python source, provider config referencing `ANTHROPIC_API_KEY` | Medium |
| **WebLLM** | In-browser LLM | 3 | hankazarian.com-beta, alignment-sphere, browse-local | Client-side LLM inference via WebGPU | Eliminates server costs for AI features; processes data locally (no data sent to external APIs); limited to smaller models (Gemma 2B, Phi-3) but sufficient for summarization and classification | `@mlc-ai/web-llm` in `package.json`, WebLLM initialization in source, worker files | Medium |
| **HuggingFace Transformers (browser)** | In-browser ML | 2 | Delta-Graph, localminds-ai | Client-side NLP (text classification, embeddings) | ONNX runtime via Transformers.js enables zero-infrastructure AI features; produces text embeddings for Voyager vector search in Delta-Graph; runs entirely in the browser via Web Workers | `@huggingface/transformers` in `package.json`, pipeline initialization in source | Medium |
| **MCP (Model Context Protocol)** | AI tool protocol | 2 | RecordsFetcher, grandandco-crm | Structured tool exposure for AI agents | Enables LLMs to call deterministic functions (records retrieval, CRM queries) with type-safe schemas; FastMCP for Python servers, MCP SDK for client connections | `fastmcp`, `mcp` in `requirements.txt`, `services/mcp-requirements.txt`, MCP server entry points in source | Medium |
| **Temporal.io** | Workflow orchestration | 2 | RecordsFetcher, grandandco-crm | Durable workflow execution for multi-step processes | Reliable retry and state recovery for long-running records fetching workflows; separates orchestration logic from business logic; used for the 5-city public records pipeline | `temporalio` in `requirements.txt`, workflow definitions in source, `docker-compose.yml` for Temporal dev server | Medium |
| **Tavily** | Web search API | 2 | linkedin-content-monitor, LinkedIn Outreach | AI-powered web search for research | Purpose-built search API for LLM agents; provides cleaned, relevant results without raw HTML parsing; used for LinkedIn content research and outreach validation | `tavily-python` in `requirements.txt`, `from tavily import TavilyClient` in source | High |
| **Capacitor** | Mobile framework | 1 | NativeEar | Cross-platform iOS/Android app from web code | Enables SvelteKit web app to run as a native mobile app with camera, microphone, and SQLite access; single codebase for both platforms | `capacitor.config.ts`, `@capacitor/core`, `@capacitor/android`, `@capacitor/ios` in `package.json`, `native-src/` directories | High |
| **Swift** | Native language | 1 | NativeEar | iOS platform code | Required by Capacitor for iOS native plugin configuration and build | `.swift` files in `native-src/` or `ios/` directory, Swift in GitHub language stats | Medium |
| **Java** | Native language | 1 | NativeEar | Android platform code | Required by Capacitor for Android native plugin configuration and build | `Java` in GitHub language stats, Android manifest files | Medium |
| **Mux** | Video platform | 1 | CineCurate | Video upload, transcoding, and playback | Handles video processing pipeline (upload → transcode → deliver) without building infrastructure; `@mux/mux-player` web component integrates directly with Svelte/Astro | `@mux/mux-player`, `@mux/mux-node` in `package.json`, Mux Playback IDs in content schema | High |
| **FastAPI** | Python web framework | 2 | linkedin-content-monitor, LinkedIn Outreach | Async REST API for content monitoring and outreach services | Async-native for concurrent request handling; automatic OpenAPI docs; Pydantic integration for request/response validation; HTMX frontend in LinkedIn Outreach for lightweight UI | `fastapi` in `requirements.txt`, `from fastapi import FastAPI`, route decorators in source, Uvicorn config | High |
| **HTMX** | Frontend library | 1 | LinkedIn Outreach | Hypermedia-driven web UI for Python backend | Eliminates need for separate JavaScript frontend; works with FastAPI's Jinja2 templates for server-rendered HTML; simpler than SPA for internal tools | Jinja2 templates with `hx-*` attributes, `fastapi` + `jinja2` in requirements, no separate JS framework | Medium |
| **Flask** | Python web framework | 2 | project-gallery, LI | Simple CRUD web application, admin panel | Lightweight enough for portfolio gallery with admin panel; Flask-Admin provides auto-generated CRUD UI; Flask-SQLAlchemy for ORM; LI uses same stack for LinkedIn asset management | `Flask>=2.3.0`, `Flask-SQLAlchemy`, `Flask-Admin` in `requirements.txt`, app factory pattern in source | High |
| **NiceGUI** | Python web framework | 1 | Linkedin-seo-this-week | Desktop-style web UI for Python analytics tool | Enables web UI without JavaScript (Python-native event handling); suited for internal tools where development speed matters more than UI polish | `nicegui` imports in `linkedin_analyzer/main.py`, UI element construction in source | High |
| **Frappe / ERPNext** | Business framework | 1 | grandandco-crm | CRM application framework | Full-featured CRM/document management system with role-based access; custom Frappe app extends base with broker-specific fields and workflows; DocType-driven data modeling | `apps/broker_ops/` custom app, Frappe DocType definitions, `hooks.py`, `frappe` imports in source | Medium |
| **Vitest** | Test framework | 12 | 1414-3rd, Delta-Graph, The-SuperFair, ai-seo-testing, fair-manager, ai-visibility-tools, cougar-gamehub, fantastic-careers, linkedin-wrapped-2025, icp-builder-pro, PatentPlayer, SmartWardrobe, Root Phaser game | Unit and integration testing for JavaScript/TypeScript projects | Native Vite integration for fast test execution; Jest-compatible API; jsdom environment for DOM-dependent tests; Vitest 4.x used across latest projects | `vitest` in `devDependencies`, `vitest.config.ts` or `vite.config.ts` (test block), `*.test.ts` or `*.spec.ts` files | High |
| **Playwright** | E2E / browser testing | 14 | The-SuperFair, fair-manager, google-ai-solar-system, growth-sprint-engine, Linkedin-seo-this-week, the-great-decoupling, linkedin-wrapped-2025, linkedin-suite, linkedin-content-monitor, icp-builder-pro, PatentPlayer, SmartWardrobe, QA Master, MantlestoneCapital | End-to-end browser testing, browser automation, accessibility auditing | Cross-browser test coverage (Chromium, Firefox, WebKit); used both for E2E testing and server-side browser automation; `@axe-core/playwright` integration in QA Master for automated accessibility compliance | `@playwright/test` in `devDependencies`, `playwright.config.ts`, `playwright` in `requirements.txt`, test spec files, axe-core integration | High |
| **pytest** | Test framework | 3 | overstock-cellars, AIVis-Bench, linkedin-suite | Unit and integration testing for Python projects | Standard Python test framework; fixture system for test isolation; parameterized tests for data validation (overstock-cellars) | `pytest>=7.0` in `pyproject.toml` or `requirements.txt`, `tests/` directory, `test_*.py` files | High |
| **Stylelint** | CSS linting | 3 | 1414-3rd, the-great-decoupling, QA Master | CSS quality enforcement | Enforces HUG CSS doctrine conventions (1414-3rd); catches CSS errors before deployment; integrated in QA Master's multi-lint pipeline | `stylelint` in `package.json`, `.stylelintrc` or config in `package.json`, stylelint commands in `scripts` | High |
| **Biome** | Linting / formatting | 2 | cac-trap-analyzer-v2, google-ai-solar-system | Unified linter and formatter for JS/TS | Single tool replaces ESLint + Prettier; significantly faster Rust-based execution; used in newer projects as ESLint alternative | `@biomejs/biome` in `devDependencies`, `biome.json` config | High |
| **ESLint** | Linting | 10 | 1414-3rd, anna-elizabeth-website, fair-manager, property-management-demo, CineCurate, kamalame-2025, art-basel-guide, localminds-ai, SmartWardrobe, opengame-evaluation, QA Master | JavaScript/TypeScript linting | Ecosystem of plugins for React, TypeScript, and Svelte; `@typescript-eslint` for strict TS rules; `react-hooks` plugin for hook dependency validation; flat config (ESLint 9+) in newer projects | `eslint` in `devDependencies`, `eslint.config.js` or `.eslintrc.*`, plugin configs | High |
| **Commander.js** | CLI framework | 1 | QA Master | CLI command structure for QA tool | Mature Node.js CLI framework with subcommand support; used for `qa-master` CLI with commands like `lint`, `audit`, `test` | `commander` in `package.json`, `import { Command } from 'commander'` in source | High |
| **axe-core** | Accessibility testing | 1 | QA Master | Automated accessibility compliance auditing | Industry standard for programmatic a11y testing; `@axe-core/playwright` integration runs axe scans via Playwright; reports violations with WCAG guidelines | `@axe-core/playwright` in `package.json`, axe-scan integration in Playwright test specs | High |
| **html-validate** | HTML validation | 1 | QA Master | HTML markup validation | Validates rendered HTML against W3C standards and custom rules; catches malformed markup before deployment | `html-validate` in `package.json`, integration in QA Master pipeline | Medium |
| **Trigger.dev** | Background jobs | 1 | CineCurate | Durable task execution for video processing | Manages async video processing pipeline (upload → thumbnail → metadata) with retries and error handling; does not require Redis or separate queue infrastructure | `@trigger.dev/sdk` in `package.json`, `trigger.config.ts`, task definitions in source | High |
| **TanStack Query** | Data fetching | 1 | media-kit (legacy) | Server state management for React | Caching, deduplication, and background refetching for API data; reduced boilerplate compared to manual `useEffect` + fetch patterns | `@tanstack/react-query` in `package.json`, QueryClient setup in source, `useQuery` hooks | Medium |
| **shadcn/ui** | Component library | 1 | property-management-demo | Copy-paste UI component system for React | Customizable components with full source ownership (not a black-box dependency); Radix UI primitives for accessibility; Tailwind CSS styling matches project's design system | `components.json`, Radix UI packages in deps (`@radix-ui/*`), `src/components/ui/` directory | High |
| **Mozilla Readability** | Content extraction | 1 | What-the-OKF | Article content extraction from HTML | Industry-standard algorithm for extracting readable content from any web page; runs in-browser; produces clean HTML input for Turndown conversion | `@mozilla/readability` in `package.json`, readability import in source | High |
| **Turndown** | HTML-to-Markdown | 1 | What-the-OKF | HTML to Markdown conversion | Handles complex HTML (tables, code blocks, lists) that manual regex conversion would miss; runs entirely in the browser | `turndown` in `package.json`, Turndown service initialized with custom rules in source | High |
| **JSZip** | ZIP generation | 2 | What-the-OKF, linkedin-wrapped-2025 | Client-side ZIP file creation | Enables download of multi-file bundles (OKF bundles, Wrapped reports) without a server; works entirely in the browser | `jszip` in `package.json`, ZIP generation in source | High |
| **Workbox** | PWA / Service worker | 1 | The-SuperFair | Offline-first service worker | Enables PWA functionality (offline access, install prompt) for the farmers market finder; precaches static assets for first-load performance | `workbox-window` in `package.json`, service worker registration in source | Medium |
| **Dexie.js** | IndexedDB wrapper | 1 | The-SuperFair | Client-side database for offline data | Simpler API than raw IndexedDB; supports indexing, queries, and transactions; stores farmers market data for offline access | `dexie` in `package.json`, Dexie database class definitions in source | High |
| **idb** | IndexedDB wrapper | 2 | cougar-gamehub, alignment-sphere | Lightweight IndexedDB wrapper | Minimal API surface for game schedule data persistence; smaller bundle than Dexie for simple key-value needs | `idb` in `package.json`, `import { openDB } from 'idb'` in source | High |
| **VoySearch** | Vector search (browser) | 2 | hankazarian.com-beta, Delta-Graph | In-browser vector similarity search | Wasm-based vector index that runs entirely on the client; enables semantic search without external API or server | `voy-search` in `package.json`, vector index initialization in source | Medium |
| **Trafilatura** | HTML extraction | 1 | research-buddy | Web page text extraction | More accurate than readability for research-quality extraction; preserves document structure; supports fallback extraction methods | `trafilatura` in `requirements.txt`, `from trafilatura import extract` in source | High |
| **Typer** | Python CLI | 1 | research-buddy | CLI argument parsing and command structure | Modern type-annotated CLI building; automatic help text and shell completion; built on Click | `typer[all]` in `pyproject.toml`, `import typer` in source | High |
| **Rich** | Terminal UI | 1 | research-buddy | Rich terminal output (tables, markdown, progress) | Colorized output, progress bars, and markdown rendering for CLI research reports | `rich` in `pyproject.toml`, `from rich import print` in source | High |
| **Ink** | Terminal React | 1 | opengame-evaluation | React-based CLI rendering | Enables React components to render in terminal; used for game evaluation CLI UI | `ink` in `package.json`, React component rendering in terminal context | Medium |
| **OpenTelemetry** | Observability | 1 | RecordsFetcher | Distributed tracing and monitoring | Traces multi-provider LLM calls and records fetching workflow steps via Logfire | `logfire`, OpenTelemetry SDKs in `requirements.txt`, instrumentation setup in source | Medium |
| **Lovable.dev** | AI web app builder | 1 | media-kit (legacy) | Vite plugin for AI-assisted TanStack Start development | Generated the initial TanStack Start scaffold and component structure; integrated via custom Vite plugin | `@lovable.dev/vite-tanstack-config` in `package.json`, `vite.config.ts` using the plugin | Medium |
| **Nitro** | Server runtime | 1 | media-kit (legacy) | SSR server bundler for TanStack Start | Handles server-side bundling and deployment for TanStack Start SSR; integrated with Bun | `nitro` in `package.json` (or as dep of `@tanstack/react-start`), Nitro config | Low |
| **Framer Motion** | Animation | 2 | kamalame-2025, MantlestoneCapital | Declarative React animations | Needed for product landing page scroll-triggered animations and micro-interactions; used in MantlestoneCapital for deck/application UI transitions | `framer-motion` in `package.json`, `motion` component imports in source | High |
| **React Hook Form** | Form management | 1 | property-management-demo | Performant form validation for React | Reduces re-renders in complex property forms; integrates with Zod via resolver for schema validation | `react-hook-form` in `package.json`, `@hookform/resolvers` for Zod, `useForm` in source | Medium |
| **Zod** | Schema validation | 5 | property-management-demo, alignment-sphere, MantlestoneCapital, SmartWardrobe, QA Master | Runtime TypeScript schema validation | Type-safe form validation that infers TypeScript types from schemas; eliminates manual type assertion; used for env config, API validation, form schemas | `zod` in `package.json`, `z.object({...})` schema definitions in source, Zod v4 in latest projects | High |
| **Recharts** | Charting | 2 | browse-local, property-management-demo | Declarative React chart components | React-native charting API; good fit for simple bar/line/pie charts in AI model comparison (browse-local) and property dashboard (property-management-demo) | `recharts` in `package.json`, `BarChart`, `LineChart` component imports | High |
| **LayerCake** | Charting | 1 | cac-trap-analyzer-v2 | Svelte-native charting framework | Svelte-first charting with composable layers (axes, data, annotations); lighter than D3 for standard chart types | `layercake` in `package.json`, `LayerCake` Svelte component imports | Medium |
| **Sonner** | Toast notifications | 1 | property-management-demo | Lightweight toast notifications | Minimal API, accessible by default, styled with Tailwind; replaces heavier alternatives like `react-hot-toast` | `sonner` in `package.json`, `<Toaster />` component in layout | Medium |
| **cheerio** | HTML parsing (Node) | 1 | art-basel-guide | Server-side HTML parsing | Lightweight jQuery-like API for extracting event data from external HTML sources; used in Next.js API routes for scraping | `cheerio` in `package.json`, `import * as cheerio from 'cheerio'` in source | High |
| **html2canvas** | Screenshot | 1 | linkedin-wrapped-2025 | Client-side DOM-to-image | Captures Wrapped report cards as PNG images for social sharing; runs entirely in the browser | `html2canvas` in `package.json`, `html2canvas()` call in source | High |
| **SheetJS (xlsx)** | Spreadsheet export | 1 | linkedin-wrapped-2025 | Excel file generation from browser | Enables download of LinkedIn data as `.xlsx` for further analysis; handles large datasets efficiently | `xlsx` in `package.json`, `XLSX.writeFile()` in source | High |
| **html2pdf.js** | PDF generation | 1 | ai-use-disclosure-prototype | Client-side HTML-to-PDF conversion | Zero-dependency PDF generation via CDN; sufficient for simple badge print layout | `<script src="html2pdf.bundle.min.js">` in HTML, CDN reference | High |
| **@imgly/background-removal** | Image processing | 1 | opengame-evaluation | Background removal from images in Node | AI-powered background removal for game asset processing pipeline | `@imgly/background-removal-node` in `package.json`, import in source | Medium |
| **esbuild** | Build tool | 1 | opengame-evaluation | Fast JavaScript bundler for Node CLI | Chosen for Ink/React terminal app bundling where Vite is not suitable; faster than webpack for Node targets | `esbuild` in `package.json`, `esbuild.config.js` | Medium |

---

## Technology Category Roll-Up

| Technology Category | Technologies Used | Distinct Projects | Capability Added |
|---|---|---|---|
| **Web application architecture** | Astro, Svelte 5, React 19, SvelteKit 2, Next.js 15/16, TanStack Start, Angular 21, TypeScript, Vite, Tailwind CSS 3/4, shadcn/ui, HTMX | 55+ | Reusable static-first and SSR products with selective client hydration; type-safe codebase across all tiers; utility-first CSS; hypermedia-driven UIs (HTMX) as lightweight alternative to SPAs |
| **AI / LLM integration** | pydantic-ai, Google Gemini API, OpenAI API, Anthropic API, WebLLM, HuggingFace Transformers (browser), Promptfoo, MCP, Temporal.io, Tavily | 17 | Structured AI agent orchestration with type-safe outputs; multi-provider LLM support; in-browser model inference (no server costs); repeatable model evaluation harness; durable multi-step AI workflows; purpose-built AI search |
| **Content management** | WordPress, Directus, Shopify, GeneratePress | 4 | Self-hosted headless CMS (Directus) for Astro content pipelines; PHP-based WordPress for client sites; Shopify for e-commerce storefronts and Liquid theme development |
| **Data storage & query** | SQLite (server + WASM), Supabase, D1 (Cloudflare), IndexedDB (Dexie, idb), sqlite-vec, VoySearch, SQLAlchemy | 15 | Embedded databases for single-process apps; in-browser SQL and vector search; BaaS auth + DB (Supabase); offline-first client-side storage; edge-distributed serverless SQL |
| **Edge infrastructure** | Cloudflare Workers/Pages, Wrangler, D1, KV, Docker | 19 | Serverless edge deployment with SSR; static asset serving with zero cold starts; containerized dev/prod environments; edge-distributed databases; multi-service orchestration |
| **Data visualization & 3D** | D3.js, Three.js/Threlte, MapLibre GL, Recharts, LayerCake | 8 | Custom SVG-based visualizations (graphs, charts, maps); 3D brand experiences; spatial data rendering for real estate; declarative chart components for dashboards |
| **Testing & quality** | Vitest, Playwright, pytest, Stylelint, Biome, ESLint, axe-core, html-validate, Commander.js | 28 | Unit/integration testing with Vite-native speed; cross-browser E2E testing; CSS quality enforcement; unified linting/formatting (Biome); Python test automation; automated accessibility auditing (axe-core); HTML validation |
| **Mobile** | Capacitor, Swift, Java, NativeEar | 1 | Cross-platform mobile app from SvelteKit web code; native device API access (camera, mic, SQLite) |
| **Game development** | Phaser 3, OpenGame SDK, Ink (terminal React) | 3 | Browser-based 2D game engine (Phaser) with scene management; OpenGame SDK for game evaluation; terminal-based game interaction via React Ink |
| **Systems / CLI** | Rust (helix-engine), Typer, Rich, Commander.js, esbuild | 5 | High-performance browser automation and image processing (Rust); typed Python CLIs; rich terminal output; Node.js CLI framework; fast bundling for Node targets |
| **Video & media** | Mux, html2canvas, html2pdf.js, JSZip, SheetJS, @imgly/background-removal | 5 | Video transcoding and playback pipeline; client-side report/image export (PDF, PNG, XLSX, ZIP); AI-powered background removal |
| **Content processing** | Mozilla Readability, Turndown, Trafilatura, Cheerio | 4 | In-browser and server-side HTML extraction and format conversion (HTML→Markdown, HTML→text, HTML→structured data) |
| **Monorepo & build orchestration** | PNPM/Turborepo, Bun, UV, Biome | 4 | Fast monorepo task execution across polyglot packages; ultra-fast package install; unified Python/Node dependency management |
| **E-commerce** | Shopify CLI, Shopify Theme Kit, Liquid | 1 | E-commerce storefront theme development with local preview; custom React storefront alongside managed Shopify theme |
| **Process / methodology** | SDD (Spec-Driven Development), 6-layer agent architecture, OpenCode | 67 (all) | Agent-governed development with spec-driven handoffs, audit trails, and deterministic validation gates across every project |

---

## Summary

| Metric | Value |
|---|---|
| **Total distinct projects reviewed** | 67 |
| **Total distinct technologies cataloged** | 78 |
| **Most frequently used technologies** | TypeScript (55+ projects), Svelte 5 (22), Astro (20), Tailwind CSS (19), Docker (16), Python 3.12 (15), React 19 (15), Playwright (14), Vitest (12), Vite (16) |
| **Technologies spanning multiple functional categories** | TypeScript, Python, SQLite (data + in-browser via WASM), Docker (infra + dev), Cloudflare (hosting + data + edge compute) |
| **New capabilities added during the year** | In-browser LLM inference (WebLLM, Transformers.js), edge SSR (Cloudflare Workers), vector search (sqlite-vec, VoySearch), AI evaluation harness (Promptfoo), cross-platform mobile (Capacitor), durable AI workflows (Temporal.io), MCP tool protocol, headless CMS (Directus), e-commerce (Shopify), game dev (Phaser 3, OpenGame), systems programming (Rust), accessibility auditing (axe-core), HTMX hypermedia UIs |
| **Build stack consolidation** | 2025 projects: Webpack, CRA, vanilla JS → 2026: Vite-first (16 projects), Bun pilot (2), Biome replacing ESLint+Prettier (2), ESLint flat config (9+) |
| **Most common deployment targets** | Cloudflare Workers/Pages (4), Docker Compose (16), Static subpath on hankazarian.com (10+), Vercel (kamalame-2025) |
| **Unverified claims** | None — all technology claims are backed by `package.json`, `pyproject.toml`, `Cargo.toml`, config files, or source imports found during audit |

### Technology Adoption Timeline (Key Shifts)

| Quarter | New Technologies Adopted |
|---|---|
| Q3 2025 (Aug–Oct) | Astro 5, Svelte 5 Runes, Cloudflare Workers, Vitest 3 |
| Q4 2025 (Nov–Jan) | Next.js 15, Tailwind CSS 4, Bun, pydantic-ai, WebLLM, Capacitor, D1, WordPress, Phaser 3 |
| Q1 2026 (Feb–Apr) | Promptfoo, MCP, Temporal.io, Biome, TanStack Start, shadcn/ui, Angular 21, Rust, Shopify, Supabase, Commander.js, axe-core, HTMX |
| Q2 2026 (May–Jul) | Astro 7, React 19 (adoption peak), sqlite-vec, VoySearch, Trigger.dev, Frappe/ERPNext, HuggingFace Transformers.js, Nitro, Directus, OpenGame SDK, Ink, Lovable.dev |
